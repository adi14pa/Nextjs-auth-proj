# Nextjs-auth-proj
A full-stack Next.js project demonstrating user authentication (login, signup) and authorization (role-based access control, protected routes).Also boilerplate for authorization systems .
